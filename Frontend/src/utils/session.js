/**
 * session.js
 * 
 * Auth is now handled via httpOnly cookies set by the backend.
 * No tokens are stored in localStorage. This file is kept as a
 * placeholder — all auth state is managed via AuthContext.
 */

// No localStorage helpers needed — cookies are httpOnly (backend-managed).
// Use useAuth() from context/AuthContext.jsx to access user state.
