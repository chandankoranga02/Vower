import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';
import { Zap } from 'lucide-react';

const HistoryPage = () => {
    return <PlaceholderPage title="Charging History" icon={<Zap size={48} />} />;
};

export default HistoryPage;
