import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';
import { Car } from 'lucide-react';

const VehiclePage = () => {
    return <PlaceholderPage title="My Vehicle" icon={<Car size={48} />} />;
};

export default VehiclePage;
